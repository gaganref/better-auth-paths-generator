{
  "name": "generate-types",
  "private": true,
  "scripts": {
    "generate": "bun run src/generate-types.ts",
    "simple": "bun run src/simple-generate-types.ts",
    "dev": "bun --watch run src/generate-types.ts",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "dependencies": {
    "@scalar/openapi-parser": "^0.18.1",
    "@scalar/openapi-types": "^0.3.5",
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "inquirer": "^9.2.12"
  },
  "devDependencies": {
    "@types/bun": "^1.2.18",
    "@types/inquirer": "^9.0.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}